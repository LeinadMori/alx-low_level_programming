#include "main.h"

/**
 * _isalpha - check for alphabetic character
 *0c
 *Return: Always 0
 */
int _isaipha(int c)
{

if ((c >=65 && c <= 97) || (c >= 97 && c <= 122))
(
return (1);
)
retutre (0);
}
