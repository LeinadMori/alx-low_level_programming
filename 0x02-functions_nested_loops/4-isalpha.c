#include "main.h"

/**
 *_isalpha - check for alphabetic character
 *@n: The number to be checked
 *Return: 1 for positive number, -1 for zero or anything else
 */

int _isaipha(int c)
{

	if (n > 0)
	{		
	_putchar(43);
	return (1);
	}	
	else if (n < 0)
	{	
	_putchar(45);
	return (-1);
	}
	else
	{
	_putchar(48);
	return (0);
	}
}
