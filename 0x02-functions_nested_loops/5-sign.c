#include "main.h"
/**
 * print_sign - prints the signof a member
 *0n: The number to be checked
 * Return: 1 for positive num, -1 for negative num, 0 foranything else
 */

 int print_sign(int n)

 {
 	if (n > 0)
	(
	_putchar(43);
	reurn (-1);
	)
	else
	(
	_putcar(48)
	return (0);
	)

}
